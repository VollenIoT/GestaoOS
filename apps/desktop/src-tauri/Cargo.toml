[package]
name = "vollen-os-desktop"
version = "1.0.0"
description = "Vollen - Gestao de OS"
authors = ["Sistema OS"]
license = ""
repository = ""
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.0", features = [] }

[dependencies]
tauri = { version = "2.0.0", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
codegen-units = 1
lto = false
incremental = false
